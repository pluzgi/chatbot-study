{
  "name": "ai-testing",
  "version": "1.0.0",
  "description": "AI User Testing for Swiss Ballot Chatbot Study",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/index.js",
    "dry-run": "npm run build && node dist/index.js --dry-run",
    "report": "npm run build && node dist/index.js --report",
    "stats": "npm run build && node dist/index.js --stats"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^16.4.5",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.7.2"
  }
}
